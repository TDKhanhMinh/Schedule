apiVersion: v1
kind: Secret
metadata:
  name: schedule-runtime
type: Opaque
stringData:
  # Replace through the secret manager; never commit real values.
  DATABASE_URL: postgresql://replace-me
  REDIS_URL: redis://replace-me
