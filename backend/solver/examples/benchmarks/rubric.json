{
  "rubricVersion": "1.0",
  "benchmarkVersion": "1.0",
  "contractVersion": "1.0",
  "seedSet": [0, 1, 7],
  "runtimeEnvironment": "local reference only; record OS, Python, OR-Tools and CPU with every run",
  "gates": {
    "hardConstraints": {
      "feasible": {
        "requiredStatus": ["OPTIMAL", "FEASIBLE"],
        "requiredHardConflictCount": 0,
        "requiredAssignmentCount": "manifest.expectedAssignmentCount"
      },
      "infeasible": {
        "requiredStatus": ["INFEASIBLE"],
        "requiredAssignmentCount": 0,
        "requiredDiagnosticSubstring": "manifest.expectedConflictContains"
      }
    },
    "runtimeSeconds": {
      "small-feasible": 5,
      "medium-near-realistic": 30,
      "infeasible-teacher-conflict": 5
    },
    "optimality": {
      "optimalStatusRequiredFor": ["small-feasible", "medium-near-realistic"],
      "maximumObjectiveGapPercent": 0,
      "currentContractNote": "SOLVER-OBJECTIVE-1.0.0 is enabled for benchmark runs; objective gap remains null because no external baseline is supplied"
    },
    "seedStability": {
      "minimumSeeds": 3,
      "stableFields": ["status", "assignmentCount", "hardConflictCount"],
      "exactAssignmentLayoutRequired": false
    },
    "explainability": {
      "infeasibleRequiresDiagnostic": true,
      "feasibleRequiresEmptyHardConflicts": true
    }
  },
  "softScoreGroups": [
    {
      "id": "teacherGap",
      "weightPercent": 20,
      "definition": "Empty periods between assignments for the same teacher/day"
    },
    {
      "id": "compactness",
      "weightPercent": 20,
      "definition": "Empty periods between assignments for the same class/day"
    },
    {
      "id": "dayDistribution",
      "weightPercent": 20,
      "definition": "Deviation from an even class load across available days"
    },
    {
      "id": "undesirableSlots",
      "weightPercent": 15,
      "definition": "Approved soft teacher availability rules that match a slot"
    },
    {
      "id": "preferredDays",
      "weightPercent": 10,
      "definition": "Approved soft rules expressing preferred days"
    },
    {
      "id": "fairness",
      "weightPercent": 15,
      "definition": "Deviation from an even teacher load across available days"
    }
  ],
  "softScorePolicy": "Use diagnostics.objectiveBreakdown.weightedTotal for the versioned objective; lower is better and hard feasibility remains a gate."
}
